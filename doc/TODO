- vyresit [Yes to All] a [No to All] (v ramci jednoho process_files())
- [Yes to All] a [No to All] (v ramci jednoho process_files()) dodelat do Alertu
      Bude to jednoduche a velmi podobne sbirani g_files/g_folders/g_bytes pro
      jednotlive oznacene polozky - tady si misto toho budu pamatovat stav
      posledniho Neco to All pro urcity dotaz (kazdy dotaz musi mit vlastni
      priznak Neco to All !!)

- zvazit moznost pouziti tmpnam() pri prohlizeni a editaci, at nepomazu omylem
      dulezite soubory v TMP. Aby bylo zachovano filename, tak doporucuji vytvaret
      v $TMP podadresar dany nahodnym jmenem (anebo treba $parcp po vzoru WC)
      a teprve do nej kopirovat soubor.

- pouzit access() misto file_exists() - uz ale nevim proc?

- pridat moznost REName do q_overwrite() ?

- pri timeoutu neodchazet z programu, ale pockat na timeout druhe strany
  a vratit se do shellu - PROC?
